{"kind": "leash", "cr": "github-89-build-axi", "leash": "auto-all", "by": "derived", "approach": ["Deliver-only mission: the build node spec + build.feature are already FROZEN (status approved) since the #84/#85 build/bundle split. #89 is the follow-up impl mission that re-implements plugin build's OUTPUT SURFACE against the frozen AXI-contract scenarios -- no spec/suite change, no spec gate. Model the output on plugin bundle's AXI form delivered in #85: default TOON table via output()+printTable, --format json structured, aggregate summary line, next-step on stderr.", "Give BuildResult per-vendor rows {vendor,path,status:built|skipped|failed} + a precomputed summary {built,skipped,failed}. build.ts: known written vendors -> built rows; unknown vendorExtensions keys -> skipped rows; per-vendor write errors -> failed rows (exit 1 if any failed). cli.ts: replace old --format json|text prose surface with default-TOON table (vendor,path,status) + 'built N, skipped M, failed K' aggregate + stderr next-step '-> universal-plugin plugin validate' emitted LAST. Empty state (no vendorExtensions) -> zero rows, aggregate 'built 0', stderr keeps 'nothing to build'.", "auto-all: self-assert the impl gate within leash after the cold sdd-impl-judge passes every frozen scenario; user ratifies in-session. No hard floor in play -- purely additive impl against a frozen contract (no narrowing/compat/conflict). On judge PASS, advance the universal-plugin project spec approved -> implemented."]}
