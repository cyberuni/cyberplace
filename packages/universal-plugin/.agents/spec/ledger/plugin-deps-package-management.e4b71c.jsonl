{"kind": "leash", "cr": "plugin-deps-package-management", "leash": "auto-none", "by": "user", "approach": ["CLEARANCE GRANTED LIVE by the owner in-session ('It is ok to unfreeze to get the behavior correctly'), scoped to the plugin/bundle node. The node is REWRITTEN, not edited: universal-plugin is a PUBLISHED npm tool that must serve any release mechanism (semantic-release included), and the frozen suite baked THIS repo's changesets policy into a general-purpose contract. Narrowed/deleted frozen scenarios include 'pins to the workspace version even when a registry would resolve differently', 'resolves from the workspace with no network access', 'a pin for a package with no workspace entry is left untouched', and the broken-workspace-entry skip. Offline support explicitly dropped by the owner.", "New model: bundle retires; plugin deps ls|up replaces it, package-manager-shaped (pnpm up / pnpm ls). The inWorkspace axis is DELETED entirely - ownership was never the real selector; the declared RANGE is. Versions resolve from a registry (--registry, default npmjs.org). Named deps rather than packages: packages/* already means the workspace glob in this repo, so 'plugin packages ls' would read as listing workspace members.", "Write semantics settled with the owner: bare npx pkg -> npx pkg@x.y.z (exact, the security default); up pkg@^2.0.0 writes the range through (owner chose pnpm-faithful over always-exact); --exact/-E resolves a range to exact (established npm/yarn name over --resolve); --latest ignores declared ranges. No lockfile exists, so a written-through range means npx resolves at RUN time - which is what makes ls's 'what does this resolve to' view load-bearing.", "Release flow inverts: pinning moves AFTER changeset publish as its own CI step over the network (root package.json 'version' script + .github/workflows/release.yml both change), replacing 'changeset version && pnpm run bundle'.", "SUBSUMES github-315 (bundle silently skips pin rewriting from the plugin dir). The reported defect is real and was reproduced (--root . from a plugin dir never walks up: path.dirname('.') === '.' terminates findMonorepoRoot on iteration one, so the workspace map comes back empty and a local package is misfiled as external, exit 0). Its fix is DELETION - the workspace discovery that carries the bug is removed - so no cwd fix is landed. Both of the issue's own hypotheses were refuted before scoping: its suspected cause (walk-up missing) is false, the walk-up exists and is defeated by a relative path; its suggested signal rule (skip count == candidate count -> nonzero exit) CONTRADICTS two frozen scenarios and would have fired the Conflict floor.", "Preserved: the pin-exempt doc-example ignore (upgrade-universal-plugin's illustration strings) must survive the rewrite unchanged.", "Reversible feature branch; hard floors still stop. Open at spec draft: .plugin/pins.json semantics under the new model; whether deps outdated splits from ls; the plugin/build sibling's framing once the dev-vs-release axis dissolves."]}
{"kind": "followup", "cr": "plugin-deps-package-management", "class": "blocking", "summary": "The spec-gate edit-class guard fails GREEN when a suite is renamed in the same change as a rewrite: classify-edit-class classifies per path, so a renamed .feature has no history at the base and every scenario reads as 'added' -> ADDITIVE, the class that self-clears and needs no re-open. Measured on this CR: the frozen bundle.feature carried 24 scenarios; rewriting it in place classifies NARROWING (0 added / 1 modified / 22 removed), but the identical deletion with a git mv to deps/deps.feature classifies ADDITIVE (40 added / 0 removed). The rename alone flips the verdict. git's own --find-renames cannot rescue it (a full rewrite leaves no similarity: bundle.feature reads as a pure 183-line delete; the unchanged sibling README.md IS detected as a rename, proving detection ran and found no match). Contradicts the completion claim that the structural classifier is what makes 'never narrow a frozen scenario without a ratified re-open' mechanically enforceable -- a Clearance-class change disguises itself as additive by renaming. Only the owner's live grant caught it on this CR. Secondary: classify-edit-class returns EGIT/UNCLASSIFIABLE for a deleted path (spawnSync ENOENT, apparently a cwd derived from the now-missing dirname), so pointing it at the OLD path is not a workaround."}
{"kind": "followup", "cr": "plugin-deps-package-management", "class": "backlog", "summary": "Delegate plugin hook init to universal-plugin (owner-identified during the explore grill; out of scope here)."}
{"kind": "followup", "cr": "plugin-deps-package-management", "class": "backlog", "summary": "The hook-npx-pin ledger's 'npx pin dormant until legion-publish' premise is stale -- cyberlegion is published (npm 0.1.0). Related: the pin-init-skill pinning path has never executed on main (the per-plugin version map exists only on the pending release PR), so verify the read-map -> --pin -> hook relay end-to-end once this CR lands."}
{"kind": "followup", "cr": "plugin-deps-package-management", "class": "blocking", "summary": "The pin-exempt doc-example protection does NOT protect the one real skill it exists for, and the corruption is already sitting in the pending release PR. The cyberspace upgrade-universal-plugin skill carries NO pin-exempt marker in its frontmatter (only name + description), so isPinExempt returns false and every version string in it is in scope. PROVEN by a real run against the corpus (reverted immediately): bundle rewrites 3 files, turning 'Found npx universal-plugin@1.2.3 in 4 files' into '@0.2.1', 'Replace all with npx universal-plugin@1.5.0? (y/n)' into '@0.2.1 (y/n)' (the pin regex also eats the trailing '?'), and the skill's core instruction into 'replace every npx universal-plugin@0.2.1 with npx universal-plugin@0.2.1' -- a self-defeating no-op. The skill's description frontmatter, which a router matches on, is corrupted too. This is NOT hypothetical: the same corruption is already committed in a6cc6cc4 on origin/changeset-release/main, one merge from shipping. Contradicts a completion claim in BOTH the retired bundle spec and this CR's new deps README, each of which asserts the upgrade-universal-plugin meta-skill IS marked pin-exempt. The frozen scenario passes only against a synthetic fixture that carries the marker; nothing ever checked the real corpus. Fix is one line (add metadata.pin-exempt: true to the skill) but it lands in a THIRD project (cyberspace) and the release PR needs the corrupted files reverted before merge."}
{"kind": "correction", "cr": "plugin-deps-package-management", "correction-kind": "refuted-premise", "cause": "The THESIS in this shard's leash line is REFUTED and must not be read as the model. It says 'the skills are the manifest' -- that any npx <pkg> reference a skill carries IS a dependency declaration -- and that 'ownership was never the real selector; the declared RANGE is'. Both are false. Proven against the real corpus and the live registry by the round-5 cold spec-judge and re-verified independently: (1) `npx skills add cyberuni/cyberplace --skill <x>` appears in 29 FILES across nearly every plugin as the standard bootstrap idiom for the UNRELATED `skills` CLI, and `skills` is a real published package (npm 1.5.19) -- a bare `deps up` would rewrite every one to `npx skills@1.5.19 add`, PINNING an installer whose entire purpose is to always fetch latest: the exact inversion of the node's security thesis. (2) The literal English prose 'shipping an npx dependency' appears in 3 governance documents (sdd-spec-judge.md, aced-spec-validator.md, aced-scenario-writer.md), and `dependency` is a real published package (npm 0.0.1) -- bare `deps up` would rewrite the SENTENCE into `npx dependency@0.0.1`. `npx` followed by a word is not a declaration; sometimes it is English, sometimes it is a different tool's command. The information needed to tell them apart is NOT IN THE STRING, so no classifier over that string can work -- which is why five consecutive judge rounds each found a new defect at a new point in the input space (R1 convergence contradiction, R2 @latest-isn't-semver, R3 @* is a truthy range, R4 satisfies('latest') is false + the unspecified extraction boundary, R5 the premise itself). Sharpening the classifier was refining an answer to the wrong question."}
{"kind": "leash", "cr": "plugin-deps-package-management", "leash": "auto-none", "by": "user", "approach": ["DIRECTION CHANGE ratified live by the owner after the round-5 Oracle failure: an ALLOWLIST replaces prose classification as the selector. .plugin/deps.json declares WHICH packages are managed; a name not on the list is invisible -- `npx skills add` and the prose 'npx dependency' are never seen, because nothing looks for them. The false-positive class does not shrink, it stops existing. Prose is still scanned, but only for names already known.", "This is a NARROW version of a model the owner previously rejected, and the difference is why it is now acceptable: the earlier proposal ALSO moved ranges into deps.json, killing prose ranges and --exact, and the owner said 'no, that's not what I meant'. Only the NAME LIST moves. Versions stay in prose, ranges stay supported (npx accepts them natively -- measured: cyberlegion@^0.1.0 resolves to 0.1.0), --exact keeps its job, ls still reads the lock. New evidence (29 files, two registry-real false positives), not a re-litigation.", "Owner also scoped IN: `deps add` / `deps remove` (the full CRUD -- how a name gets on the list), and a DETECTION command that surfaces candidate package names across a plugin's skills so the allowlist is built from evidence rather than memory. Detection is the honest answer to the bootstrapping question the judge raised twice (the old pin-exempt marker was never applied to the one skill it existed for; an ignore/allowlist entry nobody seeds fails the same way).", "SURVIVES the direction change: the extraction boundary (a reference ends at whitespace or a Markdown/sentence delimiter; the delimiter is never part of the spec) and the placeholder rule (npx cyberlegion@<version> -- cyberlegion IS managed, so <version> must still read as prose). Both still needed, but their blast radius collapses to managed names only.", "Scope CUT by the owner earlier this round and still cut: git specs, tarball URLs, file: paths and npm: aliases are out of scope -- npx's documented surface is <pkg>[@<version>].", "Hard floors still stop. The suite is NOT re-judged against this model yet; the last verdict (round 5) is ALIGNED false on the model this replaces."]}
