# identity — self-identify and discover peers

Register this session, recover its own id, and list addressable peers across the global hub. Migrated
from cyberfleet's `identity` node in `legion-extract-core` (CR-2); authored to a behavioral spec + suite
there.

> Scaffold placeholder.
