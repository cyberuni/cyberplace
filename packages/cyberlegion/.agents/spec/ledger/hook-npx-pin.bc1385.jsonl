{"kind": "leash", "cr": "hook-npx-pin", "leash": "auto-all", "by": "user", "approach": ["RE-OPEN (ratified by user plan approval): rewrite the frozen command-string scenarios from bare 'cyberlegion mail hook --event X' to the npx form 'npx cyberlegion@<version> mail hook --event X' (pinned via new init --pin) / 'npx cyberlegion mail hook --event X' (unpinned default). Touches mail/surface/surface.feature + init/init.feature.", "additive alongside: init --pin scenario + a legacy-bare-migration scenario (re-init rewrites the old bare entry in place, no duplicate)", "impl: install.ts hookCommand(event,pin); init --pin flag threaded to install; normalized upsert matcher (strip npx cyberlegion@<semver>/npx cyberlegion/bare cyberlegion prefix, compare mail-hook core) rewrites-in-place vs append", "part of approved cyberlegion surfacing-hook plan; feeds Part C (init skill reads bundle .plugin/pins.json to pass --pin). Reversible feature branch; legion-publish dependency noted (npx pin dormant until publish)", "hard floors still stop"]}
{"kind": "gate", "cr": "hook-npx-pin", "gate": "spec", "verdict": "approve", "by": "agent", "cause": "clearance", "why": "Cold sdd-spec-judge 3-lens over the re-opened + added scenarios: oracle/builder/architect all PASS, ALIGNED, no blocker. Re-open (ratified by user plan approval): surface.feature dedicated-command scenario generalized (exact bare string -> dedicated mail hook, not generic exec; cedes npx-prefix to init, removing a prior duplication); init.feature 2 SessionStart-registration scenarios rewritten bare -> npx form. Additive: --pin (npx cyberlegion@<v>), no-pin (unpinned), legacy-bare-migration (rewrite-in-place, one entry). Both features re-frozen. Non-blocking content gap: no malformed --pin validation scenario -> deferred (version comes from the trusted bundle pins map). check-spec-state OK.", "frozen": ["mail/surface/surface.feature", "init/init.feature"]}
{"kind": "gate", "cr": "hook-npx-pin", "gate": "impl", "verdict": "approve", "by": "agent", "cause": "dimension", "why": "Cold sdd-impl-judge (ADR-0016, oracle re-derived per frozen scenario): all touched scenarios PASS — init unpinned/--pin-pinned/no-pin/legacy-migration + surface dedicated-command; 295/295 cyberlegion tests (install.test 5/5, init.e2e 10/10). Exercise-backstop confirmed: make hookCommand ignore pin -> --pin test fails; drop the rewrite-in-place branch -> migration test fails (length 2 not 1); reverted. hookTarget normalizer collapses bare/unpinned/pinned to one target so re-init rewrites in place. Diff scoped to install.ts (hookCommand+hookTarget+upsert rewrite) + cli.ts (init --pin); no bleed. Full pnpm verify 19/19. Non-blocking: verify-scenarios.mts --root/--feature path bug (pre-existing SDD tooling, bridge unusable for cyberlegion -> follow-up CR); malformed --pin validation deferred.", "frozen": []}
