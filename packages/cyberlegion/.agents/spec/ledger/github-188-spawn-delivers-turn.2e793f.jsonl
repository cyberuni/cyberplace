{"kind":"leash","cr":"github-188-spawn-delivers-turn","leash":"auto-all","by":"derived","approach":["Additive scenarios on the frozen unit/lifecycle/lifecycle.feature: `unit spawn` delivers the peer's first turn on a fresh paned spawn (reuse the boot-race nudge submit-verify path), best-effort like mail send's wakeRecipient — a ring that never completes warns but the spawn succeeds; `--no-wake` opts out. Additive -> freeze self-clears, stays @frozen, no re-open of an existing scenario.","Production change: add SPAWN_DOORBELL + a best-effort wakeSpawn helper to console/doorbell.ts (rings a freshly-spawned pane via nudge, swallows failures into a warning, honors noWake), wire `--no-wake` on the `unit spawn` command and an async post-spawn best-effort ring in cli.ts mirroring mail send, add coverage, rebuild dist/cli.mjs, changeset, ADR-0026.","No frozen scenario narrowed on the cyberlegion side -> no hard floor (additive CLI opt-out flag = minor semver, under ceiling). Mechanism not routing -> CLI charter intact. Self-assert both gates within leash."]}
{"kind":"gate","cr":"github-188-spawn-delivers-turn","gate":"spec","verdict":"approve","by":"agent","cause":"dimension","why":"4 additive scenarios on the frozen unit/lifecycle/lifecycle.feature for spawn delivering the peer's first turn: happy-path first-turn ring after session open (brief still by file, doorbell wakes not re-types), boot-race re-submit delivered exactly once, best-effort ring-never-completes downgraded to a warning (spawn still succeeds), --no-wake opt-out. gherkin-cli diff addOnly:true (4 added, 0 modified/removed) -> freeze self-clears, stays @frozen, no re-open. README synced (Use-Case bullet + 3 scenario-map rows + Non-goals note for warm-pool/--visible out-of-scope). Cold sdd-spec-judge ALIGNED true (oracle/builder/architect all PASS); no open markers; mirrors the mail/doorbell best-effort-ring-atop-fail-loud-nudge precedent. Floor: none (no frozen scenario narrowed; additive CLI opt-out flag = minor semver, under ceiling).","frozen":["unit/lifecycle/lifecycle.feature"]}
