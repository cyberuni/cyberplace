{"kind": "leash", "cr": "150-nudge-boot-race", "leash": "auto-all", "by": "derived", "approach": ["REVISE unit/lifecycle nudge: make it robust to the harness boot race so a successful nudge means the peer actually took the turn, not merely staged text. Additive to the frozen lifecycle.feature (existing nudge scenarios unchanged -> self-clears, stays @frozen, no re-open).", "Chose submit-then-verify-then-retry (issue option b) over verify-readiness-before-submit (option a): adapter-general + harness-agnostic verify anchor (the caller's own staged text), fails loud on cap exhaustion. Rationale in 150-nudge-boot-race.design.md.", "impl: adapter gains a bare-Enter submit primitive (herdr pane run <id> \"\" / tmux send-keys Enter) distinct from send's text+Enter; a session-layer nudge helper sends once, reads back, resubmits the staged buffer up to a bounded cap, throws on exhaustion. Bind the new scenarios with mocked-exec tests (prior nudge scenarios landed UNBOUND).", "Low-medium blast: adapter seam + one new verb-helper + tests; no registry/worktree path touched. Reversible feature branch; hard floors still stop. Sibling cr128 also adds additive scenarios to lifecycle.feature -> mail operator on hard overlap."]}
{"kind": "gate", "cr": "150-nudge-boot-race", "gate": "spec", "verdict": "approve", "by": "agent", "cause": "dimension", "why": "Cold sdd-spec-judge 3-lens over the 4 additive nudge boot-race scenarios: oracle/builder/architect all PASS, ALIGNED true, no blocker, no open markers. gherkin-cli diff addOnly:true (4 added / 0 modified / 0 removed) -> lifecycle.feature stays @frozen, self-clears, no re-open. Existing 22 scenarios untouched. Scenarios cover verify-without-needless-retry (happy path), re-submit on boot-race swallow (the fix), no message duplication on flush, and fail-loud on cap exhaustion (no false success). Each Then boolean/observable; check-suite OK, check-spec-state OK. README nudge bullet + table synced to the suite. Non-blocking cosmetic: new scenarios assert on 'nudge' (the command) vs old 'the session adapter' (mechanism) since the verify+retry lives above the adapter; kept intentionally. Self-asserted within auto-all leash (additive, no hard floor).", "frozen": ["unit/lifecycle/lifecycle.feature"]}
{"kind": "gate", "cr": "150-nudge-boot-race", "gate": "impl", "verdict": "approve", "by": "agent", "cause": "dimension", "why": "Cold sdd-impl-judge (ADR-0016, oracle re-derived per frozen scenario) on the rebased tree (onto origin/main incl. cr128 PR #152): all 4 boot-race scenarios PASS, IMPLEMENTATION_PASS true. submit-verify-retry lives in console/nudge.ts above the adapters; bare-Enter submit primitive on SessionAdapter (herdr pane send-keys Enter / tmux send-keys Enter); cli nudge action async, awaits nudge(), fail-loud throw propagates to parseAsync().catch -> exit(1), success emit skipped. Scenario 3 confirmed retry calls adapter.submit never adapter.send (no duplication); scenario 1 no submit when first read shows turn taken; scenario 4 throws after bounded cap. Exercise-backstopped: two source mutations (submit->send, remove early-return) each broke the expected tests -> genuinely bound, closing the prior UNBOUND gap. 356/356 cyberlegion tests; root pnpm verify 19/19. Diff confined to nudge behavior, no bleed. Self-asserted within auto-all leash (no hard floor; additive, reversible feature branch).", "frozen": []}
