# agent — resolve reusable agent definitions

Resolve a named agent definition (`.agents/agents/*.md`: `model`/`effort` + the cyberlegion-only
`harness`/`warm`/`interactive` routing tags) into launch flags / a subagent instruction. `list` / `show`
/ `resolve --json` / `path`. Reads a def *file* only — no knowledge of the def's author. Authored in
`legion-agentdef` (CR-4b).

> Scaffold placeholder.
