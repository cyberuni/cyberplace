# session — warm peer session lifecycle over a multiplexer

Spawn, place (`--at pane/tab/window`), scrape, nudge, and reap a warm interactive peer via tmux/herdr.
Migrated from cyberfleet's `spawn` + `decommission` nodes in `legion-extract-core` (CR-2); the doorbell
`nudge` primitive lands with `legion-wake` (CR-4).

> Scaffold placeholder.
