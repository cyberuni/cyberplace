---
name: create-universal-plugin
description: Deprecated — use the universal-plugin skill from cyber-universal-agent-plugin instead.
metadata:
  deprecated: true
---

# create-universal-plugin (deprecated)

This skill has moved to `cyberuni/cyber-universal-agent-plugin` and is now named `universal-plugin`.

Install the replacement:

```bash
claude plugin install https://github.com/cyberuni/cyber-universal-agent-plugin.git
```

Then invoke the `universal-plugin` skill.
