---
status: draft
spec-layout:
  strategy: doc-envelope
  location: colocated
  placement-map: "#placement-map"
aligned: false
---

# platform — doc-envelope (arc42)

> Expected backfilled root (fixture). Envelope (design/, acceptance/, tooling) is the shared
> envelope from spec-layout.md; this fixture shows the strategy-distinctive body + glossary.

## Placement map

- context & scope → `context-and-scope/` (descriptive)
- the static decomposition → `building-blocks/` (descriptive)
- a runtime scenario → `runtime/` (behavioral)
- a quality scenario → `quality/` (behavioral)
- a decision + rationale → `design/decisions/` (ADR)
