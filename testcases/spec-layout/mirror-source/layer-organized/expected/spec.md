---
status: draft
spec-layout:
  strategy: capability-first
  location: colocated
  placement-map: "#placement-map"
aligned: false
---

# api — redirected to capability-first

> Expected backfilled root (fixture). Envelope (design/, acceptance/, tooling) is the shared
> envelope from spec-layout.md; this fixture shows the strategy-distinctive body + glossary.

## Placement map

- WARNED: a pure layer mirror was declined (layers carry no testable surface)
- order capability → `orders/` (layering, if any, nests inside)
- a term → `glossary/`
