---
"cyber-skills": minor
---

Add `skill-design` governance for SKILL.md authoring and `skill-repo-structure` governance for skill library repo layout. Normalize governance name input in `governance show`.

BREAKING CHANGE: Rename `discipline` CLI to `governance` and ship standards under `governances/`. Use `governance list` and `governance show <name>`. Session commit discipline is unchanged.
