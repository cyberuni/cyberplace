---
"cyberplace": patch
---

`start-mission` now names the plan brief `<cr-ref>-<what>.plan.md` so the file states what the CR does even from external sources, with the source prefix (`github`) optional. Todo `content` is a short summary (<120 chars) and the plan body stays agent-concise.
