---
"cyber-skills": minor
---

Add `--format json` flag to all dual-audience CLI commands as a more explicit alternative to `--json`. The `--json` flag is still accepted as a hidden backward-compat alias.
