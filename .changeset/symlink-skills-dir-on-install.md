---
"cyber-skills": minor
---

Create a `skills/<name>` symlink when installing a skill at project scope. If `skills/<name>` is already a real directory (an authored public skill), the symlink is skipped and a warning is printed instead.
