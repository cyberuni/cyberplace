---
"cyber-skills": patch
---

Upgrade SessionStart hooks to the current CLI semver when `hook register` finds a matching hook with an older pin. Document supply-chain install profiles and add a threat model for skill vs npm CLI trust boundaries.
