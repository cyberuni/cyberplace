---
"cyber-skills": minor
---

Replace text-input scope prompts with an arrow-key single-select TUI for `add` and `update` commands. Add `both` option to the install scope prompt so skills can be installed to project and global in one step.
