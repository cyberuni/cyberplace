---
"cyber-skills": patch
---

Remove redundant `SKILL.local.md` augmentation step from `init-commit-discipline`. Auto-commit rules are already injected into `AGENTS.md` and the SessionStart hook — no secondary reinforcement file is needed.
