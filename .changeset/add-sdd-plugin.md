---
"cyber-skills": minor
---

Add `sdd` plugin with `create-spec` and `validate-spec` skills for spec-driven development. Includes `sdd-spec-designer` and `sdd-spec-validator` agent definitions that back the skills with a quality-loop pattern.
