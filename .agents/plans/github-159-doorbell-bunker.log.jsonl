{"ts":"2026-07-12T09:02:05Z","kind":"correction","handle":"bunker","correction-kind":"judge-iteration","cause":"spec-judge-reject-then-pass","gate":"spec","note":"cold spec-judge first pass ALIGNED:false — three fixes: a Then named an internal reuse path (reworded to observable-only), --no-nudge covered peer only (added Bunker opt-out scenario), root spec.md Capabilities mail-row stale (added doorbell). Fresh cold spec-judge second pass ALIGNED:true, all three verified landed."}
{"ts":"2026-07-12T09:30:45Z","kind":"correction","handle":"bunker","correction-kind":"judge-iteration","cause":"impl-judge-reject-then-pass","gate":"impl","note":"cold impl-judge first pass IMPLEMENTATION_PASS:false — mutation-confirmed the two --no-nudge scenarios named a CLI invocation but only wakeRecipient({noNudge}) was tested, so the flag→behavior line was unguarded. Fix: added 2 mutation-resistant CLI-flag e2e tests. Also fixed a latent correctness bug found in review — selectSessionAdapter was evaluated eagerly per send and throws with no mux backend, which would fail a send from a non-mux session even to a headless recipient (violates never-fails-the-send); made adapter selection lazy inside wakeRecipient's swallowing try. Self-verified mutation resistance; re-judging."}
{"ts":"2026-07-12T10:08:46Z","kind":"correction","handle":"bunker","correction-kind":"reconcile","cause":"metaphor-leak-in-metaphor-free-package","gate":"spec","note":"Owner flagged that the packages/cyberlegion foundation must carry NO fleet/persona metaphor (its charter: pure mechanism). The Bunker rename I built leaked a persona/place name (a plugins/cyberfleet concept — the Operator's Bunker) into the package: mail bunker command, resolveBunker, persona prose. Reverted all of it — package now names only generic mechanism (standing owner / bound main pane / --owner). Kept the doorbell mechanism (metaphor-free). mail/core reverted to origin/main (0-diff). init hint made generic (--handle <name>). Re-homed the Bunker naming to a cyberfleet follow-up CR. Fresh cold spec-judge re-ALIGNED (metaphor grep clean); impl behavior unchanged, full suite + root verify green."}
