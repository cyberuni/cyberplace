---
name: migrate-skill-tooling-to-aced
status: active
todos:
  - content: "Intake: locate aced spec, scaffold plan + leash shard"
    status: completed
  - content: "Explore: scaffolded 3 new config-authoring nodes (improve-skill hybrid 32sc, list-skills 14sc, repair-private-skills 14sc) + 4 additive source-lookup scenarios on frozen contribute-skill + 2 additive manage routing scenarios"
    status: completed
  - content: "Explore: updated aced root spec.md Capability map + config-authoring/README + manage routing + regenerated by-concept; check-spec-structure clean"
    status: completed
  - content: "Cold spec-judge (aced-spec-validator) 3-lens over touched nodes — ALIGNED true, all 5 PASS, 0 blocking; 2 wording fixes applied"
    status: completed
  - content: "SPEC GATE passed — froze 3 new .feature, gate:spec ledger line, aced root status implemented->approved; check-spec-state OK"
    status: completed
  - content: "Deliver: port audit/validate.ts -> config-authoring/improve-skill/scripts/validate.mts (inline readSkillManifest, node:test); wire define-skill to call it"
    status: pending
  - content: "Deliver: port skill/source.ts -> contribute-skill/scripts/source.mts (node-only, node:test)"
    status: pending
  - content: "Deliver: port skill/list.ts (inline getPackageRoot) + skill/repair.ts -> manage engines with node:test"
    status: pending
  - content: "Rewire consumers to local scripts: aced improve-skill/define-skill/contribute-skill SKILL.md + readme; cyberspace universal-plugin/init/create-skill"
    status: pending
  - content: "cyberplace: drop auditCommand + skillCommand from cli.ts, delete src/audit + src/skill, update cyberplace readme + governances/skill-repo-structure.md"
    status: pending
  - content: "Wire aced *.test.mts into root package.json verify:specs; pnpm verify green"
    status: pending
  - content: "Impl gate (cold SDD-default impl-judge, node:test per frozen scenario) -> aced status implemented"
    status: pending
  - content: "Handoff: finalize placement (Warden scoped), branch + PR (OUTWARD-FACING, awaiting user confirm)"
    status: pending
---

# migrate-skill-tooling-to-aced — CLI skill-tooling into aced as capability nodes

Internal CR (no external source) against `.agents/specs/aced/` (status: implemented). Moves the
skill-authoring/validation engines out of the cyberplace CLI into aced as self-contained node
`.mts` engines inside skill folders (SDD convention). cyberplace keeps only marketplace + adjacent
(`awesome`/`registry`/`tavern`, plus untouched `commit`/`governance`/`hook`); its `audit` + `skill`
commands are un-spec'd legacy, so their removal is clean deletion, not a spec narrowing.

## Placement (capability-first — screaming architecture, user-approved)

- `audit/validate.ts` (S/Q/E mechanical checks) -> **config-authoring/improve-skill** — a NEW node
  that finally specs the orphan `improve-skill` skill AND lands its engine. `define-skill` calls it
  as a pre-commit gate.
- `skill/source.ts` -> **contribute/contribute-skill** engine (already its caller).
- `skill/list.ts` (inline `getPackageRoot`) + `skill/repair.ts` (validate-private + repair-private,
  writes files) -> **manage/** engines (mirrors `manage-model-runners`).
- `skill/manifest.ts` `readSkillManifest` -> inline into `validate.mts` (small, node-only already).

## Convention

Self-contained `.mts`: `node:*` only, `#!/usr/bin/env node`, pure fns exported for `node:test`,
`process.stdout.write` + exit codes (no cyberplace `output.ts`), node-≥23.6 no-deps. These are
deterministic engines -> **SDD default builder + impl-judge** (node:test), not aced rubric judges.

## Consumers to rewire (file:line from scout)

- aced: improve-skill/SKILL.md:26,29 + README.md:29; define-skill/SKILL.md:153 (`audit validate`);
  contribute-skill/SKILL.md:50 (`skill source`); aced-fit/SKILL.md:19 + readme.md:15,32 (prose).
- cyberspace: universal-plugin/SKILL.md:138; init/SKILL.md:67 (`skill repair-private`);
  create-skill/SKILL.md:69.
- cyberplace: readme.md:89,92,95,96,104,105; governances/skill-repo-structure.md:42,94,148,158.

## Ordering guard

aced gains local scripts + consumers rewired FIRST; cyberplace drops the commands LAST (same PR) so
no consumer breaks mid-flight.

## NEXT

Explore phase. Run `resolve-governances` over `.agents/universal-plugin.json` for the touched
artifact-types (skill + the deterministic `.mts` engines), then scaffold the four aced nodes with
boolean `.feature` suites and grill toward the spec gate. Start with `config-authoring/improve-skill`
(the orphan-rescue + validate engine).
