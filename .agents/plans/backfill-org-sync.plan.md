---
name: "backfill-org-sync: sync backfill-project-spec spec node + skill to the new spec organization"
overview: "Revise the authoring/backfill-project-spec unit to reflect the reorganized SDD spec corpus — the structure-doc trio (add spec-structure.md), the two-level depth cap (all strategies), and the concept axis (nodes carry concept classification; root spec.md hosts the generated by-concept index). Add the matching scenarios to the frozen .feature (additive → stays @frozen), enrich the spec README, then sync the skill impl: rebuild plugins/sdd-new/skills/backfill-project-spec from the frozen contract and fix the f9560c3 scaffold-target path bug (design/ + design/decisions/ wrongly rewritten to .agents/specs/sdd/design/). Revise CR against .agents/specs/sdd/ (status approved); artifact-type skill → ACES squad."
todos:
  - id: intake
    content: "Step 1 — open CR, scaffold this plan, emit run-level strategy (ledger seq 21, auto-all/low). DONE."
    status: completed
  - id: explore-spec
    content: "Step 2 — additive scenarios (depth-cap, by-concept block, concept-deferred) added to .feature; spec README enriched (structure trio + depth cap + concept axis). DONE."
    status: completed
  - id: spec-judge
    content: "Step 2 — cold ACES spec-judge ALIGNED 3/3, no blocker. DONE."
    status: completed
  - id: spec-gate
    content: "Step 2 gate — re-frozen; ledger seq 22; root stays approved; self-asserted. check-feature + check-spec-state green. DONE."
    status: completed
  - id: deliver-impl
    content: "Step 3 — skill synced: path bug fixed, structure trio + depth cap + by-concept block + concept-deferred in SKILL.md and plugin README. DONE."
    status: completed
  - id: impl-judge
    content: "Step 3 gate — cold impl-judge 30/30 frozen scenarios PASS, no blocker; ledger seq 23; self-asserted. DONE."
    status: completed
  - id: handoff
    content: "Step 4 — 3 commits (test spec-node; fix skill+plugin-README; docs provenance) on next; pnpm verify green. next→main PR is the owner's step."
    status: in_progress
isProject: false
---

# Plan — backfill-org-sync

> Mission plan (portable handoff brief). Tracked, per-worktree. Runs on branch `next`.
> CR: general change — "update backfill-project-spec to match the new spec organization for .agents/specs/sdd/."
> Target: `.agents/specs/sdd/` (project-path `plugins/sdd-new`), `status: approved`.
> Touched unit: `authoring/backfill-project-spec` (subject = the backfill-project-spec **skill**, artifact-type `skill` → ACES squad).

## What we are doing

The SDD corpus was reorganized: `design/spec-structure.md` is now the authoritative node-taxonomy doc
(three spec-types + the **concept axis** + the **two-level depth cap** + screaming-architecture default),
joining `spec-layout.md` (strategy menu) and `project-unit.md` (boundary/location) as a trio. The
`backfill-project-spec` unit teaches a new project this organization, so its spec + skill must reflect:
(1) the structure trio, (2) the two-level depth cap for every strategy, (3) the concept axis (nodes carry
`concept:` classification; root `spec.md` hosts a generated by-concept index block).

Separately, commit `f9560c3` introduced a path bug: it rewrote the skill's **scaffold-target** paths
(`design/`, `design/decisions/` — folders the skill creates *inside the backfilled project*) into
`.agents/specs/sdd/design/` (this repo's own corpus). Those must be generic again. Same bug in the plugin README.

## Method

- Scenario additions are **additive** (widen the contract) → fold into the frozen `.feature` under the
  conductor's authority, re-freeze at the new baseline; no `approved → draft` re-open (`lifecycle-model.md:164`).
- ACES is the squad for `skill` (spec-judge `aces-spec-validator`); cold-judge the revised suite to ALIGNED.
- Impl gate: cold static-inspection per frozen scenario (the skill is prose, not runnable code).
- Two commits at handoff per repo commit discipline.

## NEXT

Mission complete on `next`. Spec gate (ledger seq 22) + impl gate (ledger seq 23) both self-asserted
within the auto-all leash; cold spec-judge ALIGNED 3/3 and cold impl-judge 30/30. `pnpm verify` green.
Three commits landed (test spec-node / fix skill+plugin-README / docs provenance). Remaining: the
owner's `next → main` PR (carries this alongside the other open CRs). Retire this plan at doctrine retro
once merged + distilled.
