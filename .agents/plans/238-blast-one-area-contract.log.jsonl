{"ts":"2026-07-14T20:41:00Z","kind":"report","cr":"github-238","by":"agent","topic":"the-defect-is-the-contract-not-the-code","why":"Confirmed the routed defect before touching anything. Two frozen scenarios bound the same input on a 1-area project and demanded opposite answers; the blast-estimate engine's >= 2 work-area guard silently picks low, making an engine guard the de-facto spec. Verified the guard is deliberate (engine header: 'The >= 2 work areas guard on coverage is load-bearing, not a nicety') and that BOTH shipped tests already live under the precondition the contract never states: the peripheral scenario's test seeds a 3-area corpus and comments the exact contradiction ('a 1-area corpus would satisfy both scenarios' Givens at once'), and the project-wide scenario's test loops sizes 2/3/4/7, never 1. The seedFiller helper's own docstring says its purpose is to keep a corpus 'a real multi-area project rather than the degenerate 1-area shape'. So the contract was the only artifact silent on the precondition -- fixing it is a contract edit, and the engine ships unchanged."}
{"ts":"2026-07-14T20:41:00Z","kind":"report","cr":"github-238","by":"agent","topic":"option-1-is-refuted-not-merely-dispreferred","why":"The issue offered three options and called option 2 (qualify the project-wide scenario with the >= 2 precondition) the front-runner on churn. It is stronger than that: option 1 (qualify the peripheral scenario instead) would leave the project-wide scenario demanding high at size 1, where the shipped engine computes low -- so option 1 is a CODE change that flips shipped behavior, not a contract correction. Option 3 (rule 1-area projects out of scope) declined: the estimator answers correctly at size 1, so there is nothing to exclude. The cold spec-judge then strengthened the refutation independently: the old project-wide scenario contradicted THREE scenarios at size 1, not one -- the breaking-semver and surface-location scenarios also assert low on a single-area touch-set. Option 1 would have required narrowing three frozen scenarios; option 2 fixes all three with one edit."}
{"ts":"2026-07-14T20:41:00Z","kind":"report","cr":"github-238","by":"agent","topic":"the-added-scenario-is-the-load-bearing-edit","why":"Narrowing the project-wide scenario only makes the two disjoint; it leaves the size-1 answer unstated. A decision argued in an engine header but not frozen in a scenario is NOT specified, so the additive scenario ('a lone work area is its whole project but is not project-wide reach') is the edit that actually promotes the guard into the contract. It is ADDITIVE -- self-clears, stays @frozen, needs no re-open. Its Thens were verified against the SHIPPED engine by spike rather than assumed: a 1-area project touched entirely returns computed=low, projectWide=[], count=1, maxFanIn=0. The cold judge reproduced this on its own independent harness."}
{"ts":"2026-07-14T20:41:00Z","kind":"report","cr":"github-238","by":"agent","topic":"the-readme-barrier-hole-is-corroboration-loss-not-a-behavior-gap","why":"The node README claimed a barrier 'reaches across its whole project by definition, so its computed blast is high ... agree by construction'. Under the guard that has a hole at size 1, where a barrier computes low. Checked whether the barrier's escalation DEPENDS on the estimate before qualifying the claim: it does not. formation.feature drives the call-out from the Warden's own structural finding and escalates 'regardless of contract impact'; nothing in formation consults blast-estimate. The cold judge pressed this harder than the conductor did -- it hypothesized a cross-node collision between formation's self-clear and escalate scenarios activated by pinning size-1 to low, and independently disproved it: formation.feature carries no blast reference and formation/README.md disclaims the dependency outright ('the call-out is a judgment made at escalation, not a property read off a wide touch-set'). So the estimate only corroborates; at size 1 the barrier still escalates on reach and only the corroboration is lost. Qualified the claim rather than changing behavior."}
{"ts":"2026-07-14T20:44:00Z","kind":"correction","cr":"github-238","correction-kind":"judge-iteration","by":"agent","cause":"readme-glossary-and-use-case-row-gaps-found-by-cold-spec-judge","why":"Cold sdd-spec-judge round 1 returned ALIGNED true (oracle/builder/architect all PASS) but raised three CONTENT_GAPs; two were spec prose owned by explore and were fixed BEFORE the gate, so the ratified tree is not the tree the judge graded. (a) The README's Key terms glossary omitted 'coverage'/'project-wide reach' -- the exact distinction the whole CR turns on, defined only inline in prose; added it as a glossary row stating coverage is reach RELATIVE to a project as against count's absolute reach, and that a project of one has no coverage to measure. (b) The README's Use Cases table closes with 'Every scenario maps to one of these entries or to a cross-cutting guarantee', which was already FALSE for the project-wide scenario (pre-existing) and which this CR widened by one with the added scenario; added a barrier-agreement row homing both. Both edits apply the judge's own prescriptions verbatim and can only move the graded lenses toward PASS (the #192 precedent for applying a judge's prescribed changes post-verdict). The third gap -- the added scenario has no test binding -- is build-to-keep and is carried into deliver as a hard condition, not a spec fix."}
{"ts":"2026-07-14T20:44:00Z","kind":"note","cr":"github-238","by":"agent","topic":"observations-routed-not-acted-on","why":"The cold judge surfaced three typed observations; none is widened into this CR. (architect) The engine header frames the guard rationale corpus-level ('a corpus holding exactly ONE work area') while the code is project-level (areas.length < 2 on the touched project); the judge spiked a 1-area project inside a multi-project corpus and got projectWide=[] -- so the new contract is MORE precise than the engine's own prose. Reconcile the header comment in deliver (comment-only, no behavior). (architect) A 1-area project whose lone area carries CROSS-PROJECT fan-in computes medium (judge spiked maxFanIn=3); no scenario pins it -- the added scenario requires 'no fan-in', the project-wide one requires >= 2. A pre-existing gap, NOT a contradiction; the judge explicitly said do not widen this CR for it. Candidate follow-up for the owner, not auto-filed. (strategist) blast-estimate.smoke.test.mts:11 says 'green-on-all-21-scenarios'; the suite is now 22 -- stale comment, deliver cleanup."}
{"ts":"2026-07-14T20:45:00Z","kind":"note","cr":"github-238","by":"agent","topic":"align-spec-check-red-is-the-guard-firing-not-a-defect","why":"Root pnpm verify: 19/20 tasks green; the one failure is //#check:specs -> align-spec --check reporting '1 drift finding (mechanical scenario-diff): review 1 added / 1 modified / 0 removed'. Investigated rather than worked around: align-spec's DEFAULT_BASE is HEAD, not origin/main, so --check flags UNCOMMITTED scenario changes against the last commit. That is exactly this working tree pre-gate, and it is the mechanical expression of the review-before-you-commit-a-spec-change guard. It goes green once the gate commit stages the .feature (the standing rule that a spec transition stages the whole .feature). Not a defect and not a blocker; re-verified after the gate commit."}
{"ts":"2026-07-14T21:05:00Z","kind":"report","cr":"github-238","by":"agent","topic":"the-guard-the-contract-now-pins-was-itself-completely-untested","why":"Deliver bound the added scenario with a size-1 test and then MEASURED its necessity by scoped mutation rather than arguing it (#192 discipline). Removing the >= 2 guard from projectsCoveredEntirely fails EXACTLY ONE test -- the new one. The other 34 suite tests pass, and the live-corpus smoke check passes 6/6 (the real repo holds no 1-area project, so it is structurally blind to this). So before this CR the guard was not merely the de-facto spec, it was COMPLETELY UNPROTECTED: nothing in the suite or the smoke check would have caught its deletion, and the frozen scenario it silently arbitrated would then have flipped to demanding high. The test is killed by precisely the mutation naming it and by nothing incidental. Engine restored after the mutation and re-verified 35/35."}
{"ts":"2026-07-14T21:06:00Z","kind":"report","cr":"github-238","by":"agent","topic":"engine-change-is-provably-comment-only","why":"The CR claims zero engine change. Verified mechanically rather than by inspection: filtering the blast-estimate.mts diff for non-comment, non-blank added/removed lines returns EMPTY. The only engine delta is the header block, reconciled per the judge's architect observation -- the prose framed the guard corpus-level ('a corpus holding exactly ONE work area') while the code is keyed per PROJECT (areas.length < 2 on the touched project's area set), so a 1-area project inside a larger multi-project corpus is still never project-wide. The header now states the guard is the CONTRACT since #238 rather than this engine's private tiebreaker. SKILL.md carried the same corpus-level framing and the same de-facto-spec claim; reconciled identically. Root pnpm verify 21/21 green, align-spec included."}
{"ts":"2026-07-14T21:07:00Z","kind":"note","cr":"github-238","by":"agent","topic":"verify-scenarios-reports-22-of-22-UNBOUND-for-this-node-PRE-EXISTING","why":"Checked the judge's hard condition with the actual bridge rather than trusting that a test file implies a binding. verify-scenarios reports 0/22 BOUND for blast-estimate -- and that is NOT this CR's doing: it reads the SAME for the 21 scenarios #192 shipped. Root cause found rather than guessed: junitTestcaseToResult binds a testcase only via a `spec:<node>` suite segment found at any depth, and blast-estimate.test.mts uses flat `test('scenario: ...')` calls with no such wrapper, so every testcase is dropped as unbound. Wiring it means restructuring the whole test file into spec:-scoped suites with @id: tags -- a separate CR, and the cold judge explicitly said not to widen this one. The condition is met in SUBSTANCE: the added scenario has a real test that RUNS and is mutation-proven necessary (exactly one failure under the guard mutation). But the mechanical bridge reads UNBOUND, so the impl-judge must hand-reason this node's scenarios -- which is the bridge's own designed fallback for the UNBOUND set. Recorded, not hidden; candidate follow-up for the owner alongside the cross-project-fan-in gap, neither auto-filed."}
{"ts":"2026-07-14T21:40:00Z","kind":"correction","cr":"github-238","correction-kind":"judge-iteration","by":"agent","cause":"readme-1-area-claim-was-false-cr-failed-its-own-thesis","why":"Cold sdd-impl-judge round 1 returned IMPLEMENTATION_PASS FALSE on a real defect, and the finding is the CR's own thesis turned on the conductor: a change whose entire point is 'the prose must say what the code does' shipped a FALSE claim in its central reconciled paragraph. The README asserted UNQUALIFIED that on a 1-area project the estimate 'computes low' -- which drops the frozen scenario's OWN qualifier ('with no fan-in and no sensitive marking'). The .feature was careful; the prose was not. The judge falsified both halves against the real engine: a lone area MARKED sensitive computes medium, one with fan-in 6 computes medium, and one both marked and central computes HIGH -- where the barrier and the estimate DO agree, contradicting the paragraph's 'the two do not agree' as well. So the hole the issue named was MOVED, not closed: 'computes high' (false) became 'computes low' (also false -- it can be low, medium, or high). ROOT CAUSE, recorded because it is the transferable lesson: the ISSUE ITSELF carries the same over-claim, and the conductor TRANSCRIBED THE BRIEF INSTEAD OF CHECKING IT -- a correlated blind spot inherited from the source, exactly the failure mode the CR exists to fix in the suite. The conductor had verified the ENGINE claim by spike but not the PROSE claim. Fixed and this time VERIFIED EMPIRICALLY BEFORE SHIPPING, across all four corners (peripheral+unmarked->low, marked->medium, fanIn6->medium, marked+fanIn6->high): coverage never fires at size 1 so breadth stays flat, and the lone area scores on centrality and sensitivity alone -- low ONLY when peripheral and unmarked, higher when not. Also corrected 'only absolute count speaks' in SKILL.md and the engine header: true of BREADTH, false of the SCORE."}
{"ts":"2026-07-14T21:42:00Z","kind":"correction","cr":"github-238","correction-kind":"judge-iteration","by":"agent","cause":"per-project-keying-claim-newly-asserted-but-bound-by-no-test","why":"Round 1's second finding, and a genuine builder gap: the 'guard is keyed per PROJECT, not per corpus' claim is one THIS CR newly asserts in the engine header and SKILL.md, yet it was bound by no test. The judge mutated the guard to key on the TOUCH-SET's size (resolved.length < 2) instead of the touched project's area count and the producer's suite passed 41/41 -- including the new size-1 test and the smoke check -- because EVERY fixture in the file is single-project. The code was correct; the claim was undefended. A CR that asserts a property owes a test for it. Added a multi-project fixture (1-area 'atlas' + 2-area 'borealis', a 3-area touch-set covering borealis entirely) asserting only the >= 2-area project is named project-wide. Both mutations are now caught, each by exactly the test naming it: resolved.length < 2 fails only the new per-project test; removing the guard fails only the size-1 scenario test and the per-project test. Verified by running both mutations rather than by argument."}
{"ts":"2026-07-14T21:44:00Z","kind":"note","cr":"github-238","by":"agent","topic":"round-2-impl-gate-PASS-and-a-self-contradiction-closed-past-the-gate","why":"Cold sdd-impl-judge round 2 (fresh context, carrying none of round 1) returned IMPLEMENTATION_PASS TRUE: all 22 frozen scenarios hand-reasoned and passing on its OWN fixtures (fresh names solo/crowd/pair/duo, multi-project and multi-root layouts, sizes 1-7), engine diff re-proved CODE-IDENTICAL after stripping comments, zero existing-test deletions, and both mutations reproduced exactly as claimed. It went beyond the conductor twice: it added a FIFTH corner (fanIn7+unmarked -> high, since centralityScore tops at 3) proving the corrected claim is EXACTLY 'low <=> fanIn==0 AND unmarked', and it hunted NINE mutants -- five survive, all pure arithmetic thresholds, correctly judged NOT a defect because the contract fixes ordering properties rather than exact numbers and every ordering property IS bound. It also independently confirmed no consumer of projectWide exists outside the node, so the narrowing breaks nothing downstream. Its one non-blocking CONTENT_GAP was taken anyway rather than deferred: SKILL.md:46 and the engine header both carried 'the barrier agreement must hold at every project size, not only at 4+' -- a PRE-EXISTING unqualified universal, but the same class of claim that failed round 1, and one this CR's own new text contradicts ten lines away in the SAME file. Qualified both to '>= 2'. Leaving a self-contradiction inside a CR whose thesis is that a self-contradictory contract is a defect would be indefensible, and the fix is one word."}
