# workflows/ — cyberspace workflows suite

The workflows suite (cross-capability usage flows): the end-to-end path a project walks through cyberspace — **initialize**
harness-agnostic agent config, then **author**, **publish**, and **upgrade** a cross-vendor plugin. Spans
`bootstrap/` and `plugin/`; individual unit behavior lives in those capability folders, not here.

_No workflow scenarios yet._
