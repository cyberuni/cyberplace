{"kind":"leash","cr":"verify-scenarios-path-resolution","leash":"auto-all","by":"derived","approach":["deterministic .mts engine bugfix; additive path-resolution scenarios (self-clear, no re-open); isAbsolute guard at 3 join sites; no capability removed; read-only advisory tool unaffected"]}
{"kind":"gate","cr":"verify-scenarios-path-resolution","gate":"impl","node":"mission/verify-scenarios","verdict":"approve","by":"agent","why":"cold sdd impl-judge IMPLEMENTATION_PASS true, BLOCKER null (48/48; both new scenarios PASS, 29 pre-existing hold); underRoot isAbsolute guard applied at all 3 join sites (grep: join(root only inside underRoot); exercise-backstop confirmed per-site (reverting each to bare join fails its regression test). Closed the judge's non-blocking gap: getScenarioKeys (--feature) shells gherkin-cli so added a source-scan guard asserting join(root appears only in underRoot, defending all sites incl the un-unit-testable one"}
