{"seq": 1, "kind": "leash", "cr": "sdd-check-structure-breadth-vs-depth", "leash": "auto-all", "by": "derived", "blast": "low", "approach": ["revise the frozen check-spec-structure node: oversized-node finding carries a deterministic shape profile (plain/tagged counts + soft section-cluster count across both header styles) instead of prescribing a split", "add a Warden @rubric breadth-vs-depth routing arm (breadth->split, depth+deterministic->down-level via verify-scenarios, depth+agent-behavior->redesign) alongside the intra-spec-contradiction arm", "re-open ratified in-session: the oversized scenario Then-rewrite is narrowing-class (unit scenario, not acceptance); additive for the new scenarios; advisory-only, never blocks --check", "user pre-authorized the engine-hints/Warden-routes design"]}
{"seq": 2, "kind": "gate", "cr": "sdd-check-structure-breadth-vs-depth", "gate": "spec", "verdict": "approve", "by": "agent", "cause": "dimension", "frozen": ["project-spec/check-spec-structure/check-spec-structure.feature"], "why": "Cold sdd-spec-judge ALIGNED (oracle/builder/architect PASS after one boolean-form reword: 'tagged' not '@rubric' in an untagged Then). Oversized-node finding now carries a deterministic shape profile and prescribes no route; new Warden @rubric breadth-vs-depth routing arm added. Re-open of the reframed oversized unit scenario ratified in-session; self-asserted within auto-all leash."}
{"seq": 3, "kind": "gate", "cr": "sdd-check-structure-breadth-vs-depth", "gate": "impl", "verdict": "approve", "by": "agent", "cause": "dimension", "frozen": ["project-spec/check-spec-structure/check-spec-structure.feature"], "why": "Cold sdd-impl-judge IMPLEMENTATION_PASS: all deterministic + write-boundary scenarios verified by re-derived oracle + independent backstop fixture; both @rubric arms correctly ship no engine code. profileFeature computes plain/tagged/cluster counts; 21/21 node:test green (added a pipeline-level cluster-count test per the judge's observation). Self-asserted within auto-all leash."}
