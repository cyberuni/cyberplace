{"seq":1,"kind":"leash","cr":"handoff-tighten-merged-pr","leash":"auto-all","by":"derived","blast":"low","approach":["ratified re-open (user-directed, in-session clearance): narrow the frozen scenario 'a merged PR closes the source without a separate close' — its Given was unscoped ('handoff delivered the work as a pull request') while its Then ('source is closed by the merge') only holds when the source closes by reference; #130's two new scenarios exposed the latent gap","edit-class = narrowing/rewrite of a frozen scenario -> re-open (not additive) -> Clearance hard floor, pre-authorized by the user directing this follow-up; served in-session","no impl change expected: start-mission Step 4 already writes the reference only for close-by-reference sources; impl-judge re-verifies the tightened scenario against the existing realization"]}
{"seq":2,"kind":"gate","cr":"handoff-tighten-merged-pr","gate":"spec","verdict":"approve","by":"agent","cause":"clearance","frozen":["mission/handoff/handoff.feature"],"why":"Cold sdd-spec-judge ALIGNED (oracle/builder/architect PASS). Re-open (narrowing) of frozen scenario a-merged-PR-closes-the-source: Given tightened to whose source supports closing by reference so the Then (source closed by merge) is asserted only for the population that gets a Closes ref written — removes the over-broad reading #130 exposed. gherkin-cli diff modified:1 addOnly:false (genuine narrowing). Clearance pre-authorized by the user directing this follow-up, served in-session; self-asserted within auto-all leash. Judge terminology-drift observation resolved in-CR (aligned wording to sibling supports-closing-by-reference). Judge strategist observation (no scenario asserts a merged PR does NOT close a non-close-by-ref source; native disposition under-specced) filed as a separate follow-up CR. File re-frozen."}
{"seq":3,"kind":"gate","cr":"handoff-tighten-merged-pr","gate":"impl","verdict":"approve","by":"agent","cause":"dimension","frozen":["mission/handoff/handoff.feature"],"why":"Cold sdd-impl-judge IMPLEMENTATION_PASS: the tightened scenario re-verified by static inspection against the UNCHANGED realization. start-mission Step 4 writes Closes #<n> only for close-by-reference sources -> merge closes them via native auto-close, and issues no separate close (grep found no explicit issue-close call in plugins/sdd) -> satisfies both Thens. Structural cross-check against the #130 siblings + unmerged/direct-to-main scenarios: no conflict/duplication/gap. No impl delta accompanies this CR (only the .feature Given changed). Self-asserted within auto-all leash."}
