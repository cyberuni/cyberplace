{"seq":1,"kind":"leash","cr":"handoff-pr-closes-ref","leash":"auto-all","by":"derived","blast":"low","approach":["revise mission/handoff: frozen feature never asserts handoff WRITES the auto-close reference into the PR body — only that a merge closes the source; PR #120 shipped without it and its source issue never auto-closed","additive-only: add scenarios for (a) PR-flow + close-by-reference-capable source -> PR body carries the closing reference, (b) source not close-capable -> no closing reference; no existing scenario narrowed -> self-clears, stays @frozen, no re-open","sync prose: handoff README conditional-status-write-back line + impl (start-mission Step 4 + sdd-automaton) to instruct writing the source auto-close reference when the source supports it"]}
{"seq":2,"kind":"gate","cr":"handoff-pr-closes-ref","gate":"spec","verdict":"approve","by":"agent","cause":"dimension","frozen":["mission/handoff/handoff.feature"],"why":"Cold sdd-spec-judge ALIGNED (oracle/builder/architect PASS). Additive-only revise: 2 new scenarios assert handoff WRITES the source auto-close reference into the PR body (close-by-reference source -> reference naming source; non-close-capable source -> none) — closing the gap that let PR #120 ship without Closes #N. gherkin-cli diff addOnly:true (added:2, modified:0, removed:0); freeze self-clears, no re-open. README write-back prose + Use Cases row synced. Judge architect OBSERVATION (untouched frozen sibling merged-PR scenario has a loose Given) routed as a follow-up CR (tightening it narrows a frozen scenario = re-open, out of this additive scope). Self-asserted within auto-all leash."}
{"seq":3,"kind":"gate","cr":"handoff-pr-closes-ref","gate":"impl","verdict":"approve","by":"agent","cause":"dimension","frozen":["mission/handoff/handoff.feature"],"why":"Cold sdd-impl-judge IMPLEMENTATION_PASS: both new frozen scenarios verified by static inspection of the conductor realization. start-mission Step 4 unambiguously directs the positive case (close-by-reference source -> write Closes #<n> naming the source into the PR body) and the negative (non-close-capable source -> none). sdd-automaton correctly UNedited: it runs the same mission loop and holds no divergent handoff-detail copy, so inheritance is real not asserted-absent. Judge CONTENT_GAP (intake/README write-back drifted stale after handoff got precise) reconciled in-landing: descriptive non-frozen prose node, one-line consistency sync, no gate bypassed. Self-asserted within auto-all leash."}
