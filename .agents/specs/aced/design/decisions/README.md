# design/decisions/ — ADR log

Append-only architecture decision records for ACED (descriptive, ungated). The project-scope decisions facet; a behavioral unit's design fork lives in its own `<unit>.solution.md`.
