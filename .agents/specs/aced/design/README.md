# design/ — the ACED eval model

The rules/model: the four eval layers (structural, trigger, behavior, quality), the LLM-eval → agent-config mapping (test case → scenario, golden set → suite, rubric → criteria, LLM-as-judge → aced-case-judge), and the regression-gate model. Behaviors live in the capability folders.
