# workflows/ — the workflows suite

The workflows suite (cross-capability usage flows, authored in explore): the author → run → improve → compare loop, and the regression gate that blocks a config change dropping below threshold.
