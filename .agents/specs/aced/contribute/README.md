# contribute/ — propagate an authored config upstream

Sending an already-authored agent-config artifact back to its source, rather than authoring one
(`config-authoring/`) or scoring one (`eval-run/`): contribute a locally-improved installed skill to
its origin repository via pull request (`contribute-skill`).
