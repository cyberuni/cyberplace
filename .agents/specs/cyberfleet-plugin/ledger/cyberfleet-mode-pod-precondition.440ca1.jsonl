{"kind":"leash","cr":"cyberfleet-mode-pod-precondition","leash":"auto-none","by":"user","approach":["Retire the two-persona mode-switch (ADR-0022 decision 8). Operator's command-center seat is asserted by invoking the skill — no runtime `cyberfleet mode` probe, no deferral to Pod. In-ship work still reaches Pod by topical routing.","Mode becomes Pod's own precondition: marker absent -> refuse the work, ask the Council to commission, on yes run `cyberfleet init` then carry on with the original request; on no write nothing. Pod never hands off to Operator.","Correct corpus-wide marker drift to `.agents/cyberfleet/ship.json` (the code was always right; `.cyberfleet/`, `.cyberfleet/config.json` and `.agents/cyberlegion/config.json` were all wrong — ADR-0022 decision 8 is the source of the last one).","No code change: `packages/cyberfleet/src/{mode,init}.ts` and `packages/cyberfleet/.agents/spec/**` already correct and untouched. `cyberfleet mode` the command survives; only its caller set shrinks to Pod.","Record as an `## Amendment` on ADR-0022 in place — its naming and query-first decisions stand."],"why":"leash auto-none: the CR deletes two acceptance scenarios from a @frozen suite, which fires the Clearance hard floor (not self-assertable), and re-opens six more. The owner (unional) holds the user channel live this session and directed the design, so every gate verdict is human-ratified rather than agent-self-asserted."}
{"kind":"gate","cr":"cyberfleet-mode-pod-precondition","gate":"spec","verdict":"approve","by":"unional","cause":"dimension","why":"HITL-ratified live in-session (leash auto-none — Clearance hard floor, not self-assertable). The CR pivoted mid-flight from 'mode is Pod's precondition' to 'mode is deleted' (#225): the owner asked what reads the marker and the answer was nothing — `.agents/cyberfleet/ship.json` held only {version:1}, its sole reader was detectMode, detectMode's sole caller was the `cyberfleet mode` command, whose sole callers were these two personas' mode guards. Closed loop gating no capability; missions/jump/pause/gate never consulted it. Membership already lives in `cyberlegion unit register` -> AgentRecord (what `missions` enumerates, joining ship->CR on worktree.branch). CLEARANCE FLOOR: genuine deletions are the mode-switch pair (Operator activates when there is no ship marker / Operator defers to Pod when it is inside a ship), Pod defers to Operator when it is not in a ship, Operator's commission pair, and Pod's intra-mission fan-out — all moot once the switch is retired. The remaining 'removed' rows are title-keyed retitles that preserved behavior (every mechanic is a cyberlegion call -> offloaded to a CLI; a commissioned ship opens in its own workspace -> every spawned ship). Edit class vs a414b9cd: 17 added / 5 modified / 14 removed. Basis: cold ACED spec-judges over 8 rounds; final judge oracle PASS + builder PASS, architect FAIL on 7 sweep misses, all fixed (incl. plugins/cyberfleet/readme.md, the marketplace front door, which no prior round touched and still had Pod spawning) then grep-gated. pnpm verify 21/21 green. Companion shard: packages/cyberfleet/.agents/spec/ledger/cyberfleet-mode-pod-precondition.440ca1.jsonl — one CR, two touched specs.","frozen":["pod/pod.feature","operator/operator.feature"]}
{"kind":"gate","cr":"cyberfleet-mode-pod-precondition","gate":"impl","verdict":"approve","by":"unional","cause":"dimension","why":"HITL-ratified live in-session (leash auto-none — no gate self-assertable). Cold ACED impl-judge IMPLEMENTATION_PASS true across all 44 frozen scenarios (16 pod + 28 operator), oracles independently re-derived per ADR-0016, blocker null. The judge FAILED the first pass on two and verified both fixes itself: (D1) Pod's `description` never contained the word spawn, so the frozen @trigger row `start a worktree so a second agent can work the migration | no` was underivable — a harness scoring Pod reads only Pod's description and never sees Operator claim the spawn; the rule was stated in 5 places in the spec and missing from the one line a router reads. Fixed to `... not spawning ships or worktrees, fleet-wide oversight, or cross-ship routing`; judge re-walked all 9 rows (exact lexical hit on `start a worktree`, no yes-row regression) and re-confirmed the description scenario still holds (ships/worktrees appear as objects of the declined verb `spawning`, not as a location condition). (D2) `agents/headless-operator.md` + `merge-backstop-governance/{SKILL,README}.md` still asserted Pod's intra-mission fan-out — the scenario this CR deleted — because the touch set was scoped to the two persona skills; re-anchored on the Operator seat. Judge RETRACTED its own advisory to tighten `bridge work` → `fleet bridge work`, having found it would break the passing row `make the change to this project's auth capability | yes`; the weak `just refactor this file | no` row is advisory, filed as follow-up, not folded in. `pnpm verify` 21/21 green. Both suites stay @frozen. Marketplace pin needs `resync-local-plugins` after merge — it still serves the deleted `inside a ship` precondition.","frozen":["pod/pod.feature","operator/operator.feature"]}
