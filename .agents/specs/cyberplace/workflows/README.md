# workflows — cyberplace cross-capability workflows

Home for workflows (cross-capability usage flows) that span ≥2 capabilities. Empty at backfill (the narrow marketplace
scope has no cross-capability workflow yet). A future example: a user recruits a crew in the Tavern
(marketplace) and it registers into the fleet — that flow spans cyberplace + cyberfleet and would be
specced at its own seam, not invented here.
