# design — cyberplace rules & models

The rules/model home for cyberplace: cross-cutting shapes (the catalog schema, the crew facet,
CLI output formats, source-resolution layering) and the decisions log.

- [`decisions/`](./decisions/README.md) — the ADR log (append-only, descriptive, ungated).

Backfilled narrow: only entries touched by the `marketplace`/Tavern change are captured; expand by
demand.
