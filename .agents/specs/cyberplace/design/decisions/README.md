# decisions — cyberplace ADR log

Append-only, descriptive, ungated project-scope decisions. The project sibling of a unit's
`<unit>.solution.md`. Organize no node as an ADR body — this folder only logs decisions.

Seed entries (to author during the marketplace grill):

- **crew facet shape** — a reserved `crew` tag vs a new `highlights.type: crew` on the awesome
  catalog (confirm-not-guess at explore).
- **Tavern surface** — the storefront as a Starlight docs projection of the crew-filtered catalog,
  not a second governed source.
