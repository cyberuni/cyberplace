# acceptance/ — e2e behavior suite

Cross-capability outcomes (the e2e suite, authored in explore): the author → run → improve → compare loop, and the regression gate that blocks a config change dropping below threshold.
