---
spec-type: behavioral
---

# registry — register ACES as the agent-config SDD plugin

Write the aces role-map entry to .agents/universal-plugin.json so the conductor resolves ACES for agent-config artifact-types (init-aces).

## Use Cases

<!-- to be authored in explore (spec-producer) — use cases mapped to registry.feature -->
